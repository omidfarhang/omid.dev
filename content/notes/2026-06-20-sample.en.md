---
date: 2026-06-20T12:00:00+03:30
url: notes/178198396526124607/
---
This is a sample note. Add `draft: true` while writing, then remove it to publish.

Create a file, set `date` in ISO format — the URL is derived from that automatically.

Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.

Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.
Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.
Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.


Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.
Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.
Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.

Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.
Notes use simple permalinks (`/notes/hugo-notes-section/`), skip the blog RSS feed, and are omitted from per-note sitemaps.